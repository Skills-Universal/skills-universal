---
name: frontend-ui-engineering
description: Builds production-quality UIs. Use when building or modifying user-facing interfaces, creating components, implementing layouts, managing state, or when output needs to look production-quality rather than AI-generated. Use when fixing visual or UX issues, building responsive layouts, or implementing interaction patterns.
source: addyosmani/agent-skills
imported: 2026-04-29
layer: L2
---

# Frontend UI Engineering

## Overview

Build production-quality user interfaces that are accessible, performant, and visually polished. UI that looks like it was built by a design-aware engineer at a top company — not generated by an AI. Real design system adherence, proper accessibility, thoughtful interaction patterns, no generic "AI aesthetic."

## When to Use

- Building new UI components or pages
- Modifying existing user-facing interfaces
- Implementing responsive layouts
- Fixing visual or UX issues

---

## Avoid the AI Aesthetic

| AI Default | Problem | Production Quality |
|---|---|---|
| Purple/indigo everything | Models default to "safe" palettes | Use the project's actual color palette |
| Excessive gradients | Visual noise | Flat or subtle, matching design system |
| Rounded everything | Ignores hierarchy of corner radii | Consistent border-radius from design system |
| Lorem ipsum copy | Hides layout problems | Realistic placeholder content |
| Oversized padding everywhere | Destroys visual hierarchy | Consistent spacing scale |
| Shadow-heavy design | Competes with content | Subtle or no shadows |
| Generic hero sections | Template-driven, not content-first | Purpose-driven layouts |

---

## Component Architecture

- Prefer composition over configuration
- Keep components focused (one thing)
- Separate data fetching from presentation
- Avoid prop drilling deeper than 3 levels

### State Management — Choose Simplest

```
local useState       → component-specific UI state
lifted state         → shared between 2-3 siblings
Context              → theme, auth, locale (read-heavy)
URL searchParams     → filters, pagination, shareable state
Server state         → remote data with caching
Global store         → complex client state app-wide
```

---

## Design System — Spacing and Typography

**Spacing:** Use the design system scale — no arbitrary pixel values.
```css
Good: padding: 1rem;       Bad: padding: 13px;
Good: gap: 0.75rem;        Bad: margin-top: 2.3rem;
```

**Typography hierarchy:**
```
h1 → Page title (one per page)
h2 → Section title
h3 → Subsection title
body → Default text
small → Secondary/helper text
```
Don't skip heading levels. Don't use heading styles for non-heading content.

**Color:** Use semantic tokens (`text-primary`, `bg-surface`) — not raw hex. Ensure 4.5:1 contrast for normal text. Never use color as sole indicator of state.

---

## Accessibility (WCAG 2.1 AA)

- Every interactive element keyboard accessible
- ARIA labels on elements without visible text
- Focus management when content changes
- Meaningful empty, loading, and error states — never blank screens
- Never use color as sole indicator of state (use icons, text, patterns too)

---

## Responsive Design

Mobile first, then expand. Test at: 320px, 768px, 1024px, 1440px.

---

## Red Flags

- Components > 200 lines
- Inline styles or arbitrary pixel values
- Missing error/loading/empty states
- No keyboard navigation
- Generic AI look (purple gradients, oversized cards)
- Color as sole indicator of state

## Verification Checklist

- [ ] Renders without console errors
- [ ] All interactive elements keyboard accessible
- [ ] Works at 320px, 768px, 1024px, 1440px
- [ ] Loading, error, empty states all handled
- [ ] Follows project's design system
- [ ] No accessibility warnings

---
*Source: addyosmani/agent-skills — imported 2026-04-29*
